# AI Assistant Dependencies
# These packages enable LLM integration with OpenAI and Anthropic
# plus the LangGraph agent that drives Superset's in-process MCP tools.
#
# Pinned to the versions validated against this feature. Bump deliberately and
# re-test the agent (agent build, streaming, and MCP tool discovery) on upgrade.
# Install with: pip install -r requirements/ai-assistant.txt

langchain==1.3.14
langchain-core==1.5.0
langchain-openai==1.4.0
langchain-anthropic==1.5.0
langgraph==1.2.9
requests==2.32.4
